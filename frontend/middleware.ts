// Auth disabled — all routes are accessible without login.
// Re-enable by restoring next-auth middleware when auth is needed.
export default function middleware() {}

export const config = {
  matcher: [],
};
